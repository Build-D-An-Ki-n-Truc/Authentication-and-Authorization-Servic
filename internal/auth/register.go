package auth

// Claim structure
/*
	claim := jwt.MapClaims{
		"username": username,
		"role":     role,
		"exp":      time.Now().Add(time.Hour * 1).Unix(),
	}
*/

// Register an account and put in database based on where user register that from. Return true and nil if succeeded, return false and coressponding error if failed
// func RegisterAccount(username string, password string) (bool, error) {

// }
